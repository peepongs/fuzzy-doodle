name: Example CI Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Step by 25f2007872@ds.study.iitm.ac.in
        run: |
          echo "This step includes the required email address."

      - name: Run a sample command
        run: |
          echo "GitHub Actions workflow executed successfully."
